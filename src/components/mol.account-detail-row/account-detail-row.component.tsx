import * as React from "react";
import { Row } from "react-flexbox-grid";

interface IAccountDetailRowProps {}

const AccountDetailRow: React.FunctionComponent<
  IAccountDetailRowProps
> = props => {
  return <Row>Not implemeted</Row>;
};

export default AccountDetailRow;
